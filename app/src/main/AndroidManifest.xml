<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dev.mohawk.laudioapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <!-- La actividad principal sólo se podrá ver en formato vertical -->
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
        <!--
            TODO: borrar la API KEY
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".LevelChooser"
            android:label="@string/title_activity_level_chooser" />
        <activity android:name=".TrenGeltokia1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Conver_inicial" />
        <activity android:name=".Foto_completa" />
        <activity android:name=".PuzzleActivity" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".Splash" />
        <activity android:name=".TrenGeltokia2" />
        <activity android:name=".NavegacionActivity" />
        <activity android:name=".HorarioActivity" />
        <activity android:name=".TrenGeltokia3" />
        <activity android:name=".Zeramika1Activity" />
        <activity android:name=".Zeramika2Activity" />
        <activity android:name=".TrenGeltokia4"></activity>
    </application>

</manifest>